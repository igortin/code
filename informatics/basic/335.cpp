#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <math.h>

using namespace std;
int main() {
    int a,b,c,n;
    cin >> a; 
    cin >> b; 





    for ( a ; a <= b; a++ ){
        float k = sqrt(a);
        if (k - (int) k == 0) { cout << a << " ";}}
    return 0;}