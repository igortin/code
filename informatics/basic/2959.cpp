#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <cstdlib>

using namespace std;
int main() {
    int N; cin >> N; 
    if (N > 0) {cout << 1 << '\n';}
    else if (N < 0) {cout << -1 << '\n';}
	else {cout << 0 << '\n';}
	
	return 0;}